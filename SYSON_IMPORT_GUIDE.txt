#!/bin/bash
# Manual Import Guide for Syson

cat << 'EOF'
╔════════════════════════════════════════════════════════════════════════════╗
║                   SYSON SYSML MODEL IMPORT GUIDE                           ║
╚════════════════════════════════════════════════════════════════════════════╝

Your SysML model is ready to be imported into Syson!

📁 Model Location: /teamspace/studios/this_studio/agdesign/new.sysml
🎯 Project Name: AlpsRescueDroneSystem
📊 Model Size: 370 lines, 13.7 KB

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

QUICK START (Recommended):

1. Open Syson in your browser:
   👉 http://localhost:8080

2. Create a new Project:
   - Click "New Project" button
   - Name: "AlpsRescueDroneSystem"
   - Click "Create"

3. Import the SysML File:
   - Click "Add Document" or "New Document"
   - Choose "Import from file"
   - Select: /teamspace/studios/this_studio/agdesign/new.sysml
   - Click "Import"

4. View Your Model:
   - The model will appear in the editor
   - Switch between views (Text, Graphical, Form, Table)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ALTERNATIVE: Copy-Paste Method:

1. Open Syson: http://localhost:8080

2. Create a new project named "AlpsRescueDroneSystem"

3. Create a new document

4. In the text editor, paste the SysML content:
   
   a) Open terminal and display your SysML file:
      $ cat /teamspace/studios/this_studio/agdesign/new.sysml
   
   b) Copy the entire content
   
   c) Paste into Syson's text editor

5. Save the document

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MODEL CONTENTS:

Your SysML v2 model contains:

✓ Package: AlpsRescueDroneSystem
  ├─ Requirements (Functional & Non-Functional)
  │  ├─ F-01: Create system for Alps mountain rescue
  │  ├─ F-02: Detect hikers in emergency
  │  ├─ F-03: Respond with drone/UAV
  │  ├─ R-01 to R-05: Constraints (timing, visibility, control, cost, etc.)
  │
  ├─ Use Cases
  │  ├─ Hiker (actor)
  │  ├─ MountainRescuer (actor)
  │  ├─ RescueCenterOperator (extends MountainRescuer)
  │  └─ Various use case flows
  │
  └─ (Additional system model elements)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FEATURES AVAILABLE IN SYSON:

📝 Text Editor
  - View and edit SysML v2 syntax
  - Real-time validation
  - Syntax highlighting

📊 Form Editor
  - Edit model properties in forms
  - Easier for structured data

🎨 Graphical Editor
  - Visual representation of your model
  - Drag & drop capabilities
  - Connection management

📋 Table Editor
  - View/edit model elements in tables
  - Bulk operations

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TROUBLESHOOTING:

Q: Syson doesn't respond at http://localhost:8080
A: Check if containers are running:
   $ docker ps
   If not running, execute:
   $ /teamspace/studios/this_studio/agdesign/run_syson.sh

Q: Import fails
A: Try the copy-paste method instead of file import

Q: Model doesn't parse correctly
A: Check your SysML syntax. View errors in the text editor.

Q: Want to see the model file?
A: $ cat /teamspace/studios/this_studio/agdesign/new.sysml

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STATUS: ✓ Syson Ready
         ✓ SysML Model Ready
         ⏳ Awaiting Import...

Go to http://localhost:8080 to get started!

EOF
